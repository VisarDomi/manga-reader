use npx tsc --noEmit 2>&1 and npm run build to check your code.
read omp's docs for browser and edit, carefully, before continuing because these are high difficulty skills and models miss on how to correctly use them.
